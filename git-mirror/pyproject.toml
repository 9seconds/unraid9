[project]
name = "git-mirror"
version = "0.1.0"
description = "Mirror git repositories"
requires-python = ">=3.14"

authors = [
    {name = "Sergei Arkhipov", email = "nineseconds@yandex.ru"}
]

dependencies = []

[build-system]
requires = ["uv_build>=0.11.29,<0.12"]
build-backend = "uv_build"
